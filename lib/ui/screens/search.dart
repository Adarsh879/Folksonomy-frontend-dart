import 'package:flutter/material.dart';

class SearchKey extends StatelessWidget {
  const SearchKey({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
